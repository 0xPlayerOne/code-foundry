name: Code Foundry

on:
  push:
    branches: [staging]
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write

jobs:
  release-pr:
    name: Release PR
    uses: ./.github/workflows/release-pr.yml
    with:
      runner: ubuntu-slim
    secrets: inherit
