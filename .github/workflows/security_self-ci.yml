name: Security

on:
  push:
    branches: [main, staging]
  pull_request:
    branches: [staging]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  security:
    name: Security
    uses: ./.github/workflows/security.yml
    with:
      runtime-repository: 0xPlayerOne/code-foundry
      runtime-ref: v0.22.0
      runner: ubuntu-slim
    secrets: inherit
