name: CI

on:
  push:
    branches: [main, staging]
  pull_request:
    branches: [staging]
  workflow_dispatch:

jobs:
  ci:
    name: CI
    uses: 0xPlayerOne/code-foundry/.github/workflows/reusable-ci.yml@v0.6.0
    secrets: inherit
