name: CI

on:
  push:
    branches: [main, staging]
  pull_request:
    branches: [staging]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  ci:
    name: CI
    uses: 0xPlayerOne/code-foundry/.github/workflows/reusable-ci.yml@v0.7.0
    secrets: inherit
