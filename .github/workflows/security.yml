name: Security

on:
  push:
    branches: [main, staging]
  pull_request:
    branches: [staging]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  security:
    name: Security
    uses: 0xPlayerOne/code-foundry/.github/workflows/reusable-security.yml@v0.10.0
    secrets: inherit
